class Ten:

  def summ(self, number_a, number_b):
  
     if number_a in range (0, 11) and number_b in range (0, 11):
      result = number_a + number_b
      return result
      
     else:
      return -1
